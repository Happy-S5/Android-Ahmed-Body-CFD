This is for Heaven. She is Mother Nature. 

Python editor is independent. Needed on Androids to copy because IDE are not pleasant. 

I use Pycode forIDE. I run by setting settings (lockscreen) to extended. Have to select an address. I then open pycode and carefully move it in float to center. I select the icon on pycode and select splitscreen. The bottom screen is set to timer by search. I start timer. 

The above keeps mine open and running. 
